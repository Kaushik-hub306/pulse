{
  "name": "@agenttrust/pulse-core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Schedule-evaluation core for Pulse — uptime & delivery confirmation for AI agents.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
